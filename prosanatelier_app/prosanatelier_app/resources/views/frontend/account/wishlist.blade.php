@extends('layouts.store')
@section('title', 'My Wishlist - Prosan Atelier')
@section('content')
<section class="account-hero-prosan"><div class="container-fluid"><span>Saved Products</span><h1>My Wishlist</h1><p>Keep your favorite products saved and add them to cart whenever you need.</p></div></section>
<section class="account-section-prosan"><div class="container-fluid"><div class="row g-4"><div class="col-lg-3">@include('frontend.account.partials.sidebar')</div><div class="col-lg-9"><div class="account-panel-prosan"><div class="account-panel-head"><div><h3>Wishlist Products</h3><p>{{ $wishlistProducts->total() }} saved product(s).</p></div><a href="{{ route('shop.index') }}">Browse more →</a></div>@if($wishlistProducts->count())<div class="row row-cols-1 row-cols-sm-2 row-cols-xl-3 g-4">@foreach($wishlistProducts as $product)<div class="col"><div class="wishlist-card-wrap">@include('partials.product-card', ['product' => $product])<form method="POST" action="{{ route('wishlist.remove', $product) }}" class="wishlist-remove-form">@csrf @method('DELETE')<button type="submit">Remove from Wishlist</button></form></div></div>@endforeach</div><div class="pagination-wrap mt-4">{{ $wishlistProducts->links() }}</div>@else<div class="account-empty-state"><strong>Your wishlist is empty.</strong><p>Tap the heart icon on products to save them here.</p><a href="{{ route('shop.index') }}">Start shopping</a></div>@endif</div></div></div></div></section>
@endsection
